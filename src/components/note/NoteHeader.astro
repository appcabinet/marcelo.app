---
interface Props {
  title: string;
  publishedString: string;
  readingTime?: string;
}

const { title, publishedString, readingTime } = Astro.props;
---

<style>
  .note-header {
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    align-items: flex-start;
    font-weight: 400;
    margin-top: 5rem;
    margin-bottom: 3rem;
  }

  @media (max-width: 1001px) {
    .note-header {
        margin-top: 4rem;
        margin-bottom: 3rem;
    }
  }

  .note-header .title {
    font-size: var(--header-size-default);
    line-height: 1.2;
    color: var(--text-light);
    font-weight: 600;
    margin: 0;
    letter-spacing: -0.02em;
  }

  .note-header .subtitle {
    font-size: var(--text-size-default);
    line-height: var(--line-height-default);
    color: var(--text-light);
    opacity: var(--opacity-light);
    margin-top: 0rem;
    font-weight: 400;
  }

  @media (max-width: 1001px) {
    .note-header .title {
      font-size: var(--header-size-mobile);
      line-height: 1;
    }

    .note-header .subtitle {
      font-size: var(--text-size-mobile);
      line-height: 1;
    }
  }
</style>

<div class="note-header">
    <h1 class="title">{title}</h1>
    <!-- <p class="subtitle">{publishedString} {readingTime && ` · ${readingTime}`}</p> -->
</div>
